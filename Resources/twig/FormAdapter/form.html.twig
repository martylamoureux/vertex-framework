<form action="{{ action }}" method="{{ method }}">

    {{ fields|raw }}

    <button class="btn btn-success" type="submit">
        {% if model.isNew %}
            {{ form.createButtonLabel|raw }}
        {% else %}
            {{ form.saveButtonLabel|raw }}
        {% endif %}
    </button>
</form>